import { getFirestore, Timestamp, FieldValue } from 'firebase-admin/firestore';

export class salvarDados {

  constructor() {
    this.salvar();
  }

  salvar(){
      // Credenciais
      // Modulo
  }

 /* async salvarModulos() {
 
    const db = getFirestore();
try {
    const docRef = db.collection("xxxConsturtorxxx").doc("testestestse");
    return docRef.set({
       first: "Ada",
       last: "Lovelace",
       born: 1815,
     }).then(o=>{ return o})
} catch (error) {
    
}
  
  } */
}

new salvarDados();
